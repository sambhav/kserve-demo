# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/config-gitpod-file)
# and commit this file to your remote git repository to share the goodness with others.
github:
  prebuilds:
    master: true
tasks:
  - name: Setup paketo
    before: docker pull paketobuildpacks/builder:full && docker pull paketobuildpacks/run:full-cnb && chmod ugo+w /var/run/docker.sock && rm /home/gitpod/.rvm/gems/ruby-2.7.5/bin/tilt && (curl -sSL "https://github.com/buildpacks/pack/releases/download/v0.24.0/pack-v0.24.0-linux.tgz" | sudo tar -C /usr/local/bin/ --no-same-owner -xzv pack) && pack config default-builder paketobuildpacks/builder:full && pack config pull-policy if-not-present && (curl -fsSL https://raw.githubusercontent.com/tilt-dev/tilt/master/scripts/install.sh | bash)
